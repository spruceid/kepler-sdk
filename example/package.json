{
  "name": "example-kepler-dapp",
  "version": "1.0.0",
  "description": "An example Dapp using the Kepler SDK.",
  "private": true,
  "scripts": {
    "build-parent": "cd .. && yarn install && yarn build",
    "install-dependencies": "yarn build-parent && yarn install",
    "serve": "yarn install-dependencies && webpack serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ethers": "^5.6.2",
    "html-webpack-plugin": "^5.5.0",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.3",
    "webpack": "^5.71.0",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.8.0"
  },
  "dependencies": {
    "kepler-sdk": "file:.."
  }
}
